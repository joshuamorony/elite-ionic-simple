import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'elite-ionic-simple',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
